#!/usr/bin/env bash
set -e

bin/build
if [ "$#" == "0" ]; then
  node_modules/.bin/cucumber-js
  bin/lint
else
  DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search' node_modules/.bin/cucumber-js "$@"
fi
