#!/usr/bin/env bash
set -e

bin/build
if [ "$#" == "0" ]; then
  node_modules/cucumber/bin/cucumber.js
  node_modules/o-tools/bin/lint
else
  DEBUG='*,-express:*,-body-parser:*,-finalhandler,-text-stream-search' node_modules/cucumber/bin/cucumber.js "$@"
fi
