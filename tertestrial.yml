actions:
  - match:
    command: "spec"

  - match:
      filename: '\.feature$'
    command: "make spec {{filename}}"

  - match:
      filename: '\.feature$'
      line: '\d+'
    command: "make spec {{filename}}:{{line}}"
